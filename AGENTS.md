1. Always keep code simple and modular with clear separation of concerns
2. always write tests for new cude, update tests for changed code, and run all tests and fix all failures.
3. add detailed logging to a logfile that can be disabled at runtime
4. make sure code is well commented with a focus on keeping things clear for future developers
5. make sure code comments are always updated as code changes
6. always update the documentation files in a docs/ folder. If it does not exist, create it. Use a simple HTML format with lins between pages of documentation. Keep documentation organised with a comprehensive index as the front page.